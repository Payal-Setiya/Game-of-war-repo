pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                echo 'mahipal'
            }
        }
    }
    post {
        always {
            junit '**/reports/junit/*.xml'
        }
    }
}
