pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                echo 'mahipal'
            }
        }
    }
    post {
        always {
            junit **/build/test-results/test/TEST-*.xml
        }
    }
}
