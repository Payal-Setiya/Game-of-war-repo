pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                echo "hi"
            }
        }
    }
    
    post {
        always {
            junit '/var/lib/jenkins/workspace/devop/*.xml'
        }
    }

 
    
}
