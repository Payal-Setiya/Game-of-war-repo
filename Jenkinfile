pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                echo 'mahipal'
            }
        }
    }
    post {
        always {
            junit '/var/lib/jenkins/workspace/devop/report/*.xml'
        }
    }
}
