pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
               
               javac DeckOfCards.java
            }
        }

    post {
        always {            
           junit 'build/reports/**/*.xml'
        }
    }
}
