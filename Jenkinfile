pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                echo 'mahipal'
            }
        }
    }
    post {
        always {
            junit '/var/lib/jenkins/jobs/pip/builds/*/*.xml'
        }
    }
}
